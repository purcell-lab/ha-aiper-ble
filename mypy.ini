# Strict typing for the whole integration (Platinum strict-typing rule).
# CI runs exactly this configuration.
[mypy]
python_version = 3.14
strict = True
# Home Assistant re-exports platform names from package __init__ modules.
implicit_reexport = True
ignore_missing_imports = True
follow_imports = silent
files = custom_components/aiper_ble
